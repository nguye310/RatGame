class Game{
    constructor(playerOne){
        this.playerOne = playerOne //boolean player one or two
        this.gameBoard = this.makeStartingBoard()
    }

    makeStartingBoard(){
        //set the board for the player
        if(playerOne){
            //set the board for player one
        } else {
            //set the board for player two
        }
    }

    movePiece(color, shape, to){
        //color, shape
        //to: [x,y]
    }
}